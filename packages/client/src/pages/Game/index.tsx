import { CanvasWithFullscreen } from '@/components/GameField';

export const Game = () => {
  return (
    <main className='container main main-h main-bg'>
      <CanvasWithFullscreen mapName='1' />
    </main>
  );
};
